import { useState } from "react";
import projects from "../data/projects";
const filters = [["all", "Todos"], ["professional", "Profesionales"], ["personal", "Personales"], ["academic", "Académicos"]];
export default function Projects() { const [active, setActive] = useState("all"); const visible = active === "all" ? projects : projects.filter(p => p.category === active); return <section id="proyectos" className="border-y border-white/10 bg-white/[.018] px-5 py-24 sm:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-7 md:flex-row md:items-end"><div><p className="eyebrow">Portfolio</p><h2 className="section-title mt-4">Proyectos seleccionados.</h2></div><div className="flex flex-wrap gap-2">{filters.map(([value, label]) => <button key={value} onClick={() => setActive(value)} className={`rounded-full px-4 py-2 text-sm transition ${active === value ? "accent-bg text-white" : "accent-border border text-zinc-400"}`}>{label}</button>)}</div></div><div className="mt-14 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{visible.map((project, index) => <article key={project.id} className="card-surface group flex min-h-96 flex-col rounded-3xl border border-white/10 p-7 transition hover:-translate-y-1 hover:border-[#2ff77a]"><div className="flex justify-between text-sm text-zinc-500"><span>0{index + 1}</span><span>{project.year}</span></div><div className="mt-auto"><p className="accent-text mb-4 text-xs uppercase tracking-[.18em]">{filters.find(f => f[0] === project.category)[1]}</p><h3 className="text-2xl font-semibold">{project.title}</h3><p className="mt-4 leading-7 text-zinc-400">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.technologies.map(tech => <span key={tech} className="rounded-full bg-white/8 px-3 py-1 text-xs text-zinc-300">{tech}</span>)}</div><div className="mt-7 flex gap-5 text-sm"><a href={project.github} className="link-accent text-zinc-300">GitHub ↗</a><a href={project.demo} className="link-accent text-zinc-300">Demo ↗</a></div></div></article>)}</div></div></section>; }
