import { useEffect, useState } from "react";
const links = [["Inicio", "#inicio"], ["Sobre mí", "#sobre-mi"], ["Skills", "#skills"], ["Proyectos", "#proyectos"], ["Experiencia", "#experiencia"]];
const brandName = "Patrick Chavez.";
function AnimatedBrand() { const [text, setText] = useState(""); useEffect(() => { if (text.length === brandName.length) return undefined; const timer = setTimeout(() => setText(brandName.slice(0, text.length + 1)), 125); return () => clearTimeout(timer); }, [text]); return <span className="brand-typewriter">{text.slice(0, -1)}{text.endsWith(".") && <span className="accent-text">.</span>}<span className="brand-cursor" aria-hidden="true">|</span></span>; }
export default function Navbar() { const [open, setOpen] = useState(false); return <header className="fixed top-0 z-50 w-full border-b border-white/10 bg-[#030804]/85 backdrop-blur-xl"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 sm:px-8"><a href="#inicio" className="text-lg font-bold" onClick={() => setOpen(false)} aria-label="Patrick, inicio"><AnimatedBrand /></a><div className="hidden items-center gap-7 md:flex">{links.map(([name, href]) => <a key={href} href={href} className="text-sm text-zinc-400 hover:text-white">{name}</a>)}<a href="#contacto" className="accent-bg rounded-full px-4 py-2 text-sm font-semibold text-white">Contacto</a></div><button className="grid h-9 w-9 place-items-center rounded-lg border border-white/10 text-lg md:hidden" onClick={() => setOpen(!open)} aria-label="Abrir menú">{open ? "×" : "☰"}</button></nav>{open && <div className="border-t border-white/10 bg-[#030804] px-5 py-5 md:hidden"><div className="flex flex-col gap-4">{links.map(([name, href]) => <a key={href} href={href} onClick={() => setOpen(false)}>{name}</a>)}<a href="#contacto" onClick={() => setOpen(false)} className="accent-text">Contacto</a></div></div>}</header>; }
